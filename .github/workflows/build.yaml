name: Build

on: [push, workflow_dispatch]

permissions:
  contents: write # To be able to apply semantic version tags to the repository

jobs:
  build:
    name: Build
    runs-on: ubuntu-24.04

    steps:
      - id: version
        name: Get semantic version
        uses: spritely/actions.semantic-version/get@c65ae3639da17cc9ecbb985627d3890c61dec3de # v0.8.22

      - name: Apply semantic version
        if: ${{ steps.version.outputs.branchName == github.event.repository.default_branch }}
        uses: spritely/actions.semantic-version/apply@c65ae3639da17cc9ecbb985627d3890c61dec3de # v0.8.22
        with:
          version: ${{ steps.version.outputs.version }}
