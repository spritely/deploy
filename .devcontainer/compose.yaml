services:
  deploy-dev:
    container_name: deploy-dev
    image: docker.io/spritelydev/dotnet10-devcontainer:0.4.88@sha256:1e4fb4b7845bf322aeb44528cac224ad60bb6a2884ab8d798765dc9ec2e29dbf
    privileged: true
    command: /bin/sh -c "sleep infinity"
    volumes:
      - ../:/src
      - ${HOME:-$USERPROFILE}/.ssh:/root/.ssh:ro
    env_file:
      - .env
    # Environment variables have higher precedence than the .env file
    environment:
      - NUGET_TOKEN=${NUGET_TOKEN}
