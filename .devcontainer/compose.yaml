services:
  deploy-dev:
    container_name: deploy-dev
    image: docker.io/spritelydev/dotnet10-devcontainer:0.4.18@sha256:617c45b3f4aa9b4894f46f94ca9d6b2b03585d96d080015467fb2006200b0514
    privileged: true
    command: /bin/sh -c "sleep infinity"
    volumes:
      - ../:/src
      - ${HOME:-$USERPROFILE}/.ssh:/root/.ssh:ro
    env_file:
      - .env
    # Environment variables have higher precedence than the .env file
    environment:
      - NUGET_TOKEN=${NUGET_TOKEN}
